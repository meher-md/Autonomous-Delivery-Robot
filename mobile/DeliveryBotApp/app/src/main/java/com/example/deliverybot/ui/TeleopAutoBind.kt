package com.example.deliverybot.ui

/**
 * تم تعطيل كود الربط التلقائي مؤقتًا لأنه يعتمد على عناصر غير موجودة
 * (btnConnect, speedBar). لما نضيف الواجهة الخاصة بيه هنرجع الكود الحقيقي.
 */
object TeleopAutoBind
